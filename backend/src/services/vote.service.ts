// import { voteRepository } from "../repositories/vote.repository";

// export const VoteService = {
//   // Create Vote
//   createVote: async (userId: number, postId: number) => {
//     const vote = await voteRepository.createVote(userId, postId);
//     return vote;
//   },

// }